# Local Claude Code tooling config (preview server settings)
.claude/

# Original, uncompressed video sources — superseded by the compressed
# versions actually used on the site (offerte-dashboard-demo.mp4,
# linkedin-dashboard-demo.mp4). Kept locally only.
assets/Offerte dashboard.mp4
assets/LinkedIn posts dashboard.mp4
